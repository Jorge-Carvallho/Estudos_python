"""

---Em algumas outras linguagens como java e outras, o ponto de entrada(o início da execução do código) é a função main.
---No python o ponto de entrada(o ínicio da execução do código) é o modúlo, o mesmo de(o arquivo inteiro).py
---No modo interativo chamando python3 no terminal o interpretador cria um módulo vázio e qualquer variável declarada é uma variável do namespace do código.
Variaveis declaradas no corpo do módulo são variaveis globais do módulo
---Chamando globals() no terminal ele retorna um dicionário que contém todos os nomes de variáveis e objetos que foram definidos globalmente ou seja, ela não mostra as variáveis e objetos que foram definidas localmente ou (dentro de alguma outra função ou outro bloco de código)  
---OBS:Uma função definida no namespace, o value dele é uma variável globls  
---locals() retorna as variáveis locais do namespace(do módulo)



"""
